# config.yaml — choose the plugin names and their args
preprocessor:
  name: bandpass_filter
  args:
    low: 8.0
    high: 12.0
    sfreq: 1000.0
feature:
  name: band_power
  args:
    band: [8.0, 12.0]
    sfreq: 1000.0
visualizer:
  name: trial_dynamics
  args: {}
# dataset options for demo
dataset:
    n_sessions: 5
    n_channels: 4
    n_epochs: 20
    n_samples: 500  
